import ReactDOM from "react-dom"
import CyberpunkResume from "./CyberpunkResume" // Import the CyberpunkResume component

// Main app component
const App = () => {
  return <CyberpunkResume />
}

// Render the app
ReactDOM.render(<App />, document.getElementById("root"))

